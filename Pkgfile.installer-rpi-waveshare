ARG USERNAME
ARG KERNEL_VERSION
FROM ghcr.io/${USERNAME}/kernel:${KERNEL_VERSION} as kernel
FROM scratch AS customization
COPY --from=kernel /lib/modules /lib/modules

FROM ghcr.io/siderolabs/installer:v1.7.0-alpha.1
COPY --from=kernel /boot/vmlinuz /usr/install/${TARGETARCH}/vmlinuz